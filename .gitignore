# Ignore the bower_components folder that is created when bower install is run
bower_components

# Ignore the node_modules folder that is created when sudo npm install is run
node_modules
npm-debug.log

# Caching files generated by Compass
.sass-cache